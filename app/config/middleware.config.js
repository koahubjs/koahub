module.exports = {

    //http日志
    logger: true,

    //favicon设置
    favicon: 'www/favicon.ico',

    //body配置
    body: {
        multipart: true
    },

    //cors配置
    cors: false,

    //session配置
    session: false,

    //static配置
    static: false,

    //skip middleware
    skip: false,

    //http middleware
    http: false
}